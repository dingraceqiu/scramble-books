# DESIGN.md · 刷书（私人智能阅读器）

## 气质与意象
- 产品是「时装杂志的版面，知识碎片的内核」：像 LOEWE 工坊里一本装帧大胆的画册——时尚、年轻、略带古怪的艺术感。
- 意象锚点：奶油色精装画册摊开在拼布工坊的长桌上；番茄橙的便签纸压在书页上；大号无衬线字像海报标题一样砸在版面中央。
- 感觉在刷一本会动的独立杂志，而不是在用一个工具 App。
- 禁忌：中国风元素（宣纸纹理、朱砂印章、墨色书法字）一律移除；不做科技蓝紫渐变、不做纯黑纯白。

## 视觉策略
- Feed 即「小红书式笔记瀑布流」：每张卡片是一张图文笔记，上部为奶油色封面区（AI 标题用精致的无衬线粗体排版，像笔记标题而非夸张海报，如同长文被做成的图文卡片，但不是真图片），下部为正文预览。
- 卡片封面底色在一组奶油色卡之间轮换（奶白 / 杏仁 / 淡桃），像杂志内页跳色；已读卡片降饱和并压暗。
- AI 标题与原文严格分离：标题区是「编辑拟的封面大字」；正文预览区开头标注「作者原文」，正文一律不改动。
- **AI 标题只在 Feed 出现（发现入口）**：Feed 卡片封面、Feed 弹层大字报可以用 aiTitle；**Reader 连续阅读页严禁出现任何 AI 内容**（aiTitle/coreSentence/titleSupport 一律不渲染）。Reader 是书籍本来的样子——章节标题用原书章节名（SourceNode heading），正文是连续的原文段落，只允许原文 + 用户自己的划线/笔记。
- Feed 卡片**不使用任何封面图片**（含 EPUB 原生封面），统一用大字标题排版海报，重点放在标题与正文本身；EPUB 封面图只在书库书籍封面处使用。
- 古怪但好看的细节：超大字号、紧凑行高的海报标题；偶尔歪置的一枚小贴纸；编号/时长用等宽感的小字（如 NOTE 07、3 MIN）。

## 配色方案（LOEWE craft palette）
- 浅色（奶油工坊）：底 `#f4efe7`，卡片 `#fbf8f2`，文字 `#1c1a17`，次要文字 `#8a8378`，细线 `#e6dfd2`。
- 深色（暗房陈列）：底 `#13110f`，卡片 `#1e1b17`，文字 `#f2ede4`，次要文字 `#948b7e`，细线 `#2e2923`。
- 强调色：LOEWE 番茄橙 `#e85d2c`（收藏/主按钮/选中态，深色下用 `#f06a33`）；辅助芥末黄 `#d9a441`；标签底色 `#efe9dd`（深色 `#2a2620`）。
- 封面轮换色：奶白 `#fbf8f2`、杏仁 `#f3e7d3`、淡桃 `#f6ddd0`（深色对应 `#26221c`、`#2e261c`、`#33261f`）。

## 字体排版
- UI + AI 标题：Noto Sans SC。Feed 卡片标题走小红书笔记式精致字号（16-17px、font-bold、行高 1.4、紧字距 -0.01em），不要夸张海报大字；仅阅读详情弹层顶部可用更大的大字报标题。
- 正文阅读：Noto Serif SC，行高 1.9、段间距充裕（正文保留纸感阅读体验，但容器与 UI 是杂志风）。
- 标签/编号/时长：小号（11-12px）、大字距（tracking 0.08em）、可大写英文混排，如 `3 MIN`、`NOTE 07`。
- 卡片正文预览 14px、行高 1.75，限 4-5 行。

## 动效与交互
- 卡片入场：淡入 + 轻微上移（fadeUp，cubic-bezier(0.22,1,0.36,1)），瀑布流滚动加载新卡片时逐张错峰入场。
- 悬停轻微上浮 + 投影加深；收藏时爱心有一次小弹跳（scale 1 → 1.25 → 1）。
- 阅读器为全屏弹层，背景压暗模糊；所有过渡 0.25-0.4s，无弹跳闪烁。

## 布局与响应式
- 移动端优先：底部 Tab 栏（刷 Feed / 书库），Feed 两列瀑布流（小红书式）。
- 桌面端：顶部导航，Feed 3 列（超宽 4 列）；阅读器最大宽度 3xl 居中。
- Feed 使用无限滚动，每次渲染 12 张，滚到底自动追加——支撑数百上千单元的大书不卡顿。

## 卡片操作（精简）
- 笔记卡片上只保留两个操作：爱心（收藏）、「不感兴趣」（少推荐这个主题，点击后标记「已减少」）。
- 不做点赞、不做含义不明的手势按钮；划线与笔记仅在阅读器内使用。

## 内容切分宗旨（产品逻辑，非视觉）
- 切分必须是「完整的论点 / 案例 / 论证单元」：读者只看一篇就能知道作者在讲什么，禁止无头无脑的碎片。
- 每个阅读单元以章节标题开头（单元自带上下文）；每章切成 2-4 个大单元；宁可大而完整，不可短而破碎。
