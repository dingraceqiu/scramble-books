# GUI 纵切验证 · 自动化结果与真书人工验收清单

> 2026-09-05。自动化部分：本地生成 3 本结构差异书 + 浏览器自动化（IAB）走查；
> 人工部分：待用真实书籍复核（真实渲染环境 + 人眼）。
> 环境备忘：测试服务 `PORT=5321 pnpm tsx server/server.ts`；测试书生成脚本思路见会话记录（标准 EPUB / 脏结构 EPUB / 长文 TXT）。

---

## 一、自动化走查结果（合成书）

| # | 检查项 | 结果 |
|---|--------|------|
| 1 | 导入 3 本结构差异书（标准 EPUB / 脏结构 EPUB / 长文 TXT），解析、分类、书库展示 | ✓ 全部成功；书籍类型自动归类社科；覆盖率/章节数/单元数即时显示 |
| 2 | Feed 瀑布流：双列卡片、AI 标题（mock）、NOTE 编号、阅读时长、来源署名、双操作按钮 | ✓ 与 DESIGN.md 一致 |
| 3 | Feed 弹层：大字标题与原文严格分区（"Original text · unedited" 标注）、来源信息、收藏/多推荐/少推荐/Not today/Continue/Random next | ✓ |
| 4 | **滚到底标记已读**：弹层滚至 ≥95% → readRanges 落库（via='feed'）→ 覆盖率实时更新 | ✓（写入 ch#0..5、6 节点，Feed 头部与书库同步变化） |
| 5 | 覆盖率口径统一：Feed 头部（聚合节点口径）、书库卡片（60%）、Reader 顶栏（20%）、目录章节条（100%）全部同源一致 | ✓ |
| 6 | Feed→Reader 互认：Feed 读过的章节，Reader 内对应正文段落全部带 r-read 已读样式 | ✓（5/5 正文段落，标题节点按 Exposure 语义计） |
| 7 | 持久化：reload 后书目/阅读状态/覆盖率完整恢复 | ✓ |
| 8 | **重切分不变性（GUI 级）**：书 A 切换社科→小说，单元全部重建（"5 episodes (serialized in Feed)"），readRanges 18 节点/3 区间/60% 覆盖率分毫不动 | ✓ |
| 9 | 重切分后 Study：KP 资格不变（同一 sourceRanges 窗口仍在册）、已读笔记列表切换为 Ep.1-3 小说序号 | ✓ |
| 10 | Study 统计与 TD-01 窗口抽取：已读区域自动产出 KP（quote=原文逐字、来源=窗口区间） | ✓ |
| 11 | 测验（Quiz 出题→答题→Mastery→跳回原文） | ⚠️ 未走通：合成书只产出 1-2 个 KP，不足出题最低配置（1 题干+3 干扰项=4 KP）；出题逻辑本身有 2709 项契约断言覆盖 |

### 发现并已修复
- **P1（已修，commit 8aa1164）**：Feed 头部 `0%% covered` 双百分号，且该处是残留的「单元口径」实现——已改为与全局同源的节点口径聚合。

### 记录不修（观察项）
- **P2 UX**：KP 不足 4 个时点击「Start quiz」无任何反馈（静默拒绝）。建议后续加一行提示（如「再读 N 篇即可出题」）。
- **P2 数据**：TXT 元数据启发式——标题取文件名首段（`长文-长夜漫记.txt` → 标题「长文」），首行内容被当作者。对真实 TXT 书需要复核规则。
- **观察**：脏结构 EPUB（12 个 1-2 段小章 + 列表章 + 空段）合并后仅产出 1 个 Feed 单元——符合「宁可大而完整」的设计，但接近上限，真书复核时关注同类书的切分观感。

### 环境限制（非应用缺陷）
- 内嵌浏览器在宿主窗口被遮挡时会暂停 rAF 与 IntersectionObserver：弹层的恢复流程（rAF）卡住会吞掉滚动标记（解释了测试初期的假象）；Reader 的 IO 滚动上报因此无法在此环境验证——其写入路径（addReadRanges/合并/派生）与 Feed 侧共用且已验证，IO 触发本身留待真书人工复核。
- 后台标签页中 CSS 动画层截图呈现为黑块（合成器伪影），DOM 与 computed style 均正常。

---

## 二、真书人工验收清单（2–3 本真实书，建议 30 分钟）

测试书选择：① 一本标准 EPUB（正常章节结构）；② 一本结构脏的 EPUB（短章/小节多/列表/空段）；③ 一本长篇 TXT 或「其他」类型书。**PDF 不在当前支持面内，不用测。**

准备：`PORT=5321 pnpm tsx server/server.ts`（或 `pnpm dev`）→ 浏览器打开。每步勾选；任何一步不符合预期，记下书名+步骤号。

### A. 导入与切分
- [ ] 1. 三本书先后导入，全部解析成功，书库出现卡片（封面/标题/作者/章节·篇数）
- [ ] 2. 自动分类合理；类型按钮可手动改（改完 Feed 内容随之变化）
- [ ] 3. Feed 卡片数量观感：每本书的「篇」是完整论点/案例，无无头无尾的碎片（重点看脏结构书）

### B. Feed 阅读
- [ ] 4. 打开一张卡：标题区是 AI 大字、正文标注「原文未改动」、来源书名·作者可见
- [ ] 5. 弹层滚到底后关闭 → 卡片出现已读徽标；Feed 头部覆盖率数字上升
- [ ] 6. 中途退出（滚一半）→ 重开该卡恢复到上次位置
- [ ] 7. 爱心收藏 →「Saved」过滤可见；「不感兴趣」→ 卡片消失
- [ ] 8. 「Loved it（多推荐）」可点，无报错
- [ ] 9. 「Continue this book」弹层内切下一篇；「Random next」随机换一篇
- [ ] 10. 快速连点卡片/滚动，无白屏、无卡片跳栏重排

### C. Reader 连续阅读
- [ ] 11. 从弹层「Back to the book」进入 Reader：顶栏百分比 = 书库该书的覆盖率（同一数字）
- [ ] 12. 目录里刚读过的章节显示 100%，其余 0%；顶栏 = 各章加权和
- [ ] 13. **滚动阅读一段后**：顶栏百分比上升（IO 上报）；回 Feed 该书已读篇数增加
- [ ] 14. 字号/字体/行距/三主题切换即时生效；深色主题下划线颜色正常
- [ ] 15. 四色划线 + 写一条笔记 → 「Highlights & notes」列表可见
- [ ] 16. 全文搜索命中并高亮；书签添加/删除正常
- [ ] 17. 退出 Reader → 重开 → 恢复到上次滚动位置

### D. 重切分不变性（核心）
- [ ] 18. 记下书 A 当前覆盖率 → 书库改书籍类型 → 覆盖率数字**不变**
- [ ] 19. 已读徽标对应同一批原文位置（章节对得上，不要求篇号对上）
- [ ] 20. Reader 内已读样式/划线仍在同一原文位置
- [ ] 21. 新单元若含 70% 已读 + 30% 未读，不显示「完全已读」徽标（部分覆盖语义正确）

### E. Study 学习闭环
- [ ] 22. 读满 4 篇以上后进 Study：统计（Books/Notes read/阅读时长）合理
- [ ] 23. 知识点自动出现（数量 ≥1），点开可见 quote 原文与来源章节
- [ ] 24. Start quiz 出题（4 选项）→ 作答 → 正确率/Attempts 更新（1 次作答 = 初步证据，不显示 100% 掌握）
- [ ] 25. 题目「回到原文/查看依据」跳转 Reader 对应位置并高亮闪烁
- [ ] 26. 划线/笔记出现在 Study 的汇总列表，点击可跳回原文

### F. 持久化与语言
- [ ] 27. 全程后刷新页面：书目/进度/划线/笔记/KP 全部还在
- [ ] 28. 中英切换：全部界面文案切换、无中英混杂残留；刷新后语言保持

### G. 云端（可选，需要部署环境 + 邀请码）
- [ ] 29. 注册/登录 → 云端拉取或推送 → 第二台设备/浏览器登录后数据一致
- [ ] 30. 登出时「保留/清空本地」两条路径各自符合预期

## 三、发现缺陷怎么记

按 `P0 阻断 / P1 功能错 / P2 体验` 分级，记录：书名、步骤号、预期 vs 实际、截图。P0/P1 直接进入下一轮修复；P2 汇总后统一排期。
