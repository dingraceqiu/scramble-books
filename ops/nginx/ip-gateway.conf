# Stable host-level shell. Project deploys must not rewrite this file.
server {
    listen 80;
    listen [::]:80;
    server_name 129.204.30.165;
    root /var/www;

    include /etc/nginx/project-locations/*.conf;

    location / {
        try_files $uri $uri/ =404;
    }
}
